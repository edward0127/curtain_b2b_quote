<section class="hero hero--compact">
  <div>
    <p class="eyebrow">New <%= quote_or_order_label %></p>
    <h2>Build Multi-Item Curtain <%= quote_or_order_label %></h2>
    <p class="lead">Select products, dimensions, and quantity for each line. Pricing is applied automatically using active catalog rules.</p>
  </div>
</section>

<section class="panel">
  <%= render "form", quote_request: @quote_request %>
</section>
