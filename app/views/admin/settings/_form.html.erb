<% return_to_dashboard = local_assigns.fetch(:return_to_dashboard, false) %>
<% dashboard_compact = local_assigns.fetch(:dashboard_compact, false) %>

<% if app_setting.errors.any? %>
  <div class="flash flash--alert">
    <strong><%= pluralize(app_setting.errors.count, "error") %> prevented saving:</strong>
    <ul>
      <% app_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: app_setting, url: admin_settings_path, method: :patch do |form| %>
  <% if return_to_dashboard %>
    <%= hidden_field_tag :return_to, "dashboard" %>
    <% if dashboard_compact %>
      <%= hidden_field_tag :compact, "1" %>
    <% end %>
  <% end %>

  <h3>Business Settings</h3>
  <div class="form-grid">
    <div class="field field--full">
      <%= form.label :pickup_address_default, "Default pickup address" %>
      <%= form.text_field :pickup_address_default, required: true %>
      <small>Shown in pickup emails and order communications.</small>
    </div>

    <div class="field field--full">
      <%= form.label :delivery_note_default, "Default delivery note" %>
      <%= form.text_field :delivery_note_default, required: true %>
      <small>Shown in pickup/delivery communications.</small>
    </div>
  </div>

  <h3>Invoice Bank Details</h3>
  <p class="muted">These details are shown on customer invoice emails and invoice PDFs.</p>
  <div class="form-grid">
    <div class="field">
      <%= form.label :bank_account_name, "Account name" %>
      <%= form.text_field :bank_account_name %>
    </div>

    <div class="field">
      <%= form.label :bank_name, "Bank name" %>
      <%= form.text_field :bank_name %>
    </div>

    <div class="field">
      <%= form.label :bank_bsb, "BSB" %>
      <%= form.text_field :bank_bsb %>
    </div>

    <div class="field">
      <%= form.label :bank_account_number, "Account number" %>
      <%= form.text_field :bank_account_number %>
    </div>
  </div>

  <h3>Portal Email Settings</h3>
  <div class="form-grid">
    <div class="field">
      <%= form.label :mail_from_email %>
      <%= form.email_field :mail_from_email, required: true %>
      <small>Sender address shown on portal emails.</small>
    </div>

    <div class="field">
      <%= form.label :quote_receiver_email, "Shared admin inbox email" %>
      <%= form.email_field :quote_receiver_email, required: true %>
      <small>Internal order notifications will be sent to this address.</small>
    </div>
  </div>

  <h3>Advanced Email Delivery (Technical setup)</h3>
  <p class="muted">These SMTP fields are technical and usually only need changes during email configuration.</p>
  <div class="form-grid">
    <div class="field">
      <%= form.label :mailgun_domain %>
      <%= form.text_field :mailgun_domain, required: true %>
    </div>

    <div class="field">
      <%= form.label :mailgun_smtp_username %>
      <%= form.text_field :mailgun_smtp_username, required: true %>
    </div>

    <div class="field">
      <%= form.label :mailgun_smtp_password %>
      <%= form.password_field :mailgun_smtp_password %>
    </div>

    <div class="field">
      <%= form.label :mailgun_smtp_address %>
      <%= form.text_field :mailgun_smtp_address, required: true %>
    </div>

    <div class="field">
      <%= form.label :mailgun_smtp_port %>
      <%= form.number_field :mailgun_smtp_port, min: 1, required: true %>
    </div>
  </div>
  <p class="muted">Leave SMTP password blank to keep the current value.</p>

  <h3>Portal URL</h3>
  <div class="form-grid">
    <div class="field">
      <%= form.label :app_host %>
      <%= form.text_field :app_host, required: true %>
    </div>

    <div class="field">
      <%= form.label :app_port %>
      <%= form.number_field :app_port, min: 1, required: true %>
    </div>

    <div class="field">
      <%= form.label :app_protocol %>
      <%= form.select :app_protocol, options_for_select([ "http", "https" ], app_setting.app_protocol), required: true %>
    </div>
  </div>

  <h3>Public Website Typography</h3>
  <p class="muted">Global defaults for heading and body font. These are also used as fallback for the partners page inline editor.</p>

  <div class="form-grid">
    <div class="field">
      <%= form.label :public_heading_font, "Heading font" %>
      <%= form.select :public_heading_font, options_for_select(AppSetting::PUBLIC_FONT_OPTIONS.to_a, app_setting.public_heading_font.presence || AppSetting::DEFAULTS[:public_heading_font]), required: true %>
    </div>

    <div class="field">
      <%= form.label :public_body_font, "Body font" %>
      <%= form.select :public_body_font, options_for_select(AppSetting::PUBLIC_FONT_OPTIONS.to_a, app_setting.public_body_font.presence || AppSetting::DEFAULTS[:public_body_font]), required: true %>
    </div>
  </div>

  <p class="muted">
    To edit website page text and images, open
    <%= link_to "/edit", edit_pages_path, data: { turbo_frame: "_top" } %>.
  </p>

  <div class="actions">
    <%= form.submit "Save settings", class: "button button--primary" %>
  </div>
<% end %>
